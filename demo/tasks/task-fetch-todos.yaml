apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: task-fetch-todos
  namespace: default
spec:
  type: fetch
  http:
    method: GET
    url: "https://jsonplaceholder.typicode.com/todos?userId={{input.userId}}"
    headers:
      Accept: "application/json"
  inputSchema:
    type: object
    properties:
      userId:
        type: integer
        description: User ID to filter todos by
    required:
      - userId
  outputSchema:
    type: array
    items:
      type: object
      properties:
        userId:
          type: integer
        id:
          type: integer
        title:
          type: string
        completed:
          type: boolean
