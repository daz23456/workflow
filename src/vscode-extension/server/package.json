{
  "name": "workflow-vscode-server",
  "version": "0.1.0",
  "description": "Language server for Workflow YAML files",
  "main": "./dist/server.js",
  "scripts": {
    "build": "webpack --mode production",
    "type-check": "tsc --noEmit",
    "watch": "webpack --mode development --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@workflow/types": "workspace:*",
    "@workflow/validation": "workspace:*",
    "@workflow/graph": "workspace:*",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "typescript": "^5.9.3",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4",
    "ts-loader": "^9.5.1"
  }
}
