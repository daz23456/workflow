apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: adjuststock
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: AdjustStock
  annotations:
    workflow.io/source-path: /api/inventory/adjust
    workflow.io/source-method: POST
spec:
  type: http
  description: POST /api/inventory/adjust
  request:
    url: http://localhost:5100/api/inventory/adjust
    method: POST
    headers:
      Accept: application/json
      Content-Type: application/json
    body: '{{input.body | toJson}}'
  inputSchema:
    type: object
    properties:
      body:
        type: object
        properties:
          productId:
            type: string
          adjustment:
            type: integer
            format: int32
          reason:
            type: string
        additionalProperties: false
        description: Request body
    required:
      - body
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
