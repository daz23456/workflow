apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: bulkcheckavailability
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: BulkCheckAvailability
  annotations:
    workflow.io/source-path: /api/inventory/bulk-check
    workflow.io/source-method: POST
spec:
  type: http
  description: POST /api/inventory/bulk-check
  request:
    url: http://localhost:5100/api/inventory/bulk-check
    method: POST
    headers:
      Accept: application/json
      Content-Type: application/json
    body: '{{input.body | toJson}}'
  inputSchema:
    type: object
    properties:
      body:
        type: object
        properties:
          items:
            type: array
            items:
              type: object
              properties:
                productId:
                  type: string
                quantity:
                  type: integer
                  format: int32
              additionalProperties: false
        additionalProperties: false
        description: Request body
    required:
      - body
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
