apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: cancelsubscription
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: CancelSubscription
  annotations:
    workflow.io/source-path: /api/payments/subscription/{id}
    workflow.io/source-method: DELETE
spec:
  type: http
  description: DELETE /api/payments/subscription/{id}
  request:
    url: http://localhost:5100/api/payments/subscription/{{input.id}}
    method: DELETE
    headers:
      Accept: application/json
  inputSchema:
    type: object
    properties:
      id:
        type: string
        description: 'Path parameter: id'
    required:
      - id
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
