apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: capturepayment
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: CapturePayment
  annotations:
    workflow.io/source-path: /api/payments/capture
    workflow.io/source-method: POST
spec:
  type: http
  description: POST /api/payments/capture
  request:
    url: http://localhost:5100/api/payments/capture
    method: POST
    headers:
      Accept: application/json
      Content-Type: application/json
    body: '{{input.body | toJson}}'
  inputSchema:
    type: object
    properties:
      body:
        type: object
        properties:
          paymentId:
            type: string
          amount:
            type: number
            format: double
        additionalProperties: false
        description: Request body
    required:
      - body
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
