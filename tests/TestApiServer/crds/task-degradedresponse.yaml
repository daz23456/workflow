apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: degradedresponse
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: DegradedResponse
  annotations:
    workflow.io/source-path: /api/retry/degraded
    workflow.io/source-method: POST
spec:
  type: http
  description: POST /api/retry/degraded
  request:
    url: http://localhost:5100/api/retry/degraded
    method: POST
    headers:
      Accept: application/json
      Content-Type: application/json
    body: '{{input.body | toJson}}'
  inputSchema:
    type: object
    properties:
      body:
        type: object
        properties:
          delayMs:
            type: integer
            format: int32
        additionalProperties: false
        description: Request body
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
