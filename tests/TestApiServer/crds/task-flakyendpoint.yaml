apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: flakyendpoint
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: FlakyEndpoint
  annotations:
    workflow.io/source-path: /api/retry/flaky
    workflow.io/source-method: POST
spec:
  type: http
  description: POST /api/retry/flaky
  request:
    url: http://localhost:5100/api/retry/flaky
    method: POST
    headers:
      Accept: application/json
      Content-Type: application/json
    body: '{{input.body | toJson}}'
  inputSchema:
    type: object
    properties:
      body:
        type: object
        properties:
          key:
            type: string
        additionalProperties: false
        description: Request body
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
