apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: getpricing
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: GetPricing
  annotations:
    workflow.io/source-path: /api/inventory/pricing/{id}
    workflow.io/source-method: GET
spec:
  type: http
  description: GET /api/inventory/pricing/{id}
  request:
    url: http://localhost:5100/api/inventory/pricing/{{input.id}}
    method: GET
    headers:
      Accept: application/json
  inputSchema:
    type: object
    properties:
      id:
        type: string
        description: 'Path parameter: id'
      quantity:
        type: integer
        format: int32
        description: 'Query parameter: quantity'
    required:
      - id
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
