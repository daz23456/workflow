apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: getuserpreferences
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: GetUserPreferences
  annotations:
    workflow.io/source-path: /api/users/{id}/preferences
    workflow.io/source-method: GET
spec:
  type: http
  description: GET /api/users/{id}/preferences
  request:
    url: http://localhost:5100/api/users/{{input.id}}/preferences
    method: GET
    headers:
      Accept: application/json
  inputSchema:
    type: object
    properties:
      id:
        type: string
        description: 'Path parameter: id'
    required:
      - id
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
