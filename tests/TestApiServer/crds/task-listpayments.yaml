apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: listpayments
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: ListPayments
  annotations:
    workflow.io/source-path: /api/payments
    workflow.io/source-method: GET
spec:
  type: http
  description: GET /api/payments
  request:
    url: http://localhost:5100/api/payments
    method: GET
    headers:
      Accept: application/json
  inputSchema:
    type: object
    properties:
      userId:
        type: string
        description: 'Query parameter: userId'
      status:
        type: string
        description: 'Query parameter: status'
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
