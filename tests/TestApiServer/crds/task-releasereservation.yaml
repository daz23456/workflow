apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: releasereservation
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: ReleaseReservation
  annotations:
    workflow.io/source-path: /api/inventory/reserve/{id}
    workflow.io/source-method: DELETE
spec:
  type: http
  description: DELETE /api/inventory/reserve/{id}
  request:
    url: http://localhost:5100/api/inventory/reserve/{{input.id}}
    method: DELETE
    headers:
      Accept: application/json
  inputSchema:
    type: object
    properties:
      id:
        type: string
        description: 'Path parameter: id'
    required:
      - id
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
