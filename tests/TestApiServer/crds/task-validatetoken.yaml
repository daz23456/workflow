apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: validatetoken
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: ValidateToken
  annotations:
    workflow.io/source-path: /api/users/validate-token
    workflow.io/source-method: POST
spec:
  type: http
  description: POST /api/users/validate-token
  request:
    url: http://localhost:5100/api/users/validate-token
    method: POST
    headers:
      Accept: application/json
      Content-Type: application/json
    body: '{{input.body | toJson}}'
  inputSchema:
    type: object
    properties:
      body:
        type: object
        properties:
          token:
            type: string
        additionalProperties: false
        description: Request body
    required:
      - body
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
