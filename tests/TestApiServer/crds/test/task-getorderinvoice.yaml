apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: getorderinvoice
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: GetOrderInvoice
  annotations:
    workflow.io/source-path: /api/orders/{id}/invoice
    workflow.io/source-method: GET
spec:
  type: http
  description: GET /api/orders/{id}/invoice
  request:
    url: http://localhost:5100/api/orders/{{input.id}}/invoice
    method: GET
    headers:
      Accept: application/json
  inputSchema:
    type: object
    properties:
      id:
        type: string
        description: 'Path parameter: id'
    required:
      - id
  outputSchema:
    type: object
    description: Response from the endpoint
  timeout: 30s
  retry:
    maxAttempts: 3
    backoffMs: 1000
