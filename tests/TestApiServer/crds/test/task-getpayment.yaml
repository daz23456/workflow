apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: getpayment
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: GetPayment
  annotations:
    workflow.io/source-path: /api/payments/{id}
spec:
  type: http
  http:
    url: "http://localhost:5100/api/payments/{id}"
    method: GET
  inputSchema:
    type: object
    properties:
      id:
        type: string
  outputSchema:
    type: object
