apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: getuserprofile
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: GetUserProfile
  annotations:
    workflow.io/source-path: /api/users/{id}/profile
spec:
  type: http
  http:
    url: "http://localhost:5100/api/users/{id}/profile"
    method: GET
  inputSchema:
    type: object
    properties:
      id:
        type: string
  outputSchema:
    type: object
