apiVersion: workflow.example.com/v1
kind: WorkflowTask
metadata:
  name: listtransactions
  namespace: test
  labels:
    workflow.io/generated-from: openapi
    workflow.io/operation-id: ListTransactions
  annotations:
    workflow.io/source-path: /api/payments/transactions
spec:
  type: http
  http:
    url: "http://localhost:5100/api/payments/transactions"
    method: GET
  inputSchema:
    type: object
    properties:
      userId:
        type: string
      from:
        type: string
      to:
        type: string
  outputSchema:
    type: object
